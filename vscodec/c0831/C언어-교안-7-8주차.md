# C언어 교안 (7-8주차)

**목표:**
- C 언어 문법 이해
- 배열·함수·포인터 개념 습득
- 작업형 평가 문제 해결 가능
- 간단한 콘솔 프로그램 제작

---

## 📅 7주차 – 배열 (★중요)

### 📌 학습 목표
- 1차원 배열 선언과 사용
- 배열과 반복문의 조합
- 배열을 이용한 알고리즘 구현

### 📘 이론

#### 7.1 배열이란?
- **같은 자료형의 변수들을 연속적으로 저장**
- 인덱스로 접근 (0부터 시작)
- 메모리에 연속적으로 저장됨

#### 7.2 배열 선언

```c
// 방법 1: 크기 지정
int arr[5];

// 방법 2: 초기화와 함께
int arr[5] = {1, 2, 3, 4, 5};

// 방법 3: 크기 생략 (초기값 개수만큼)
int arr[] = {1, 2, 3, 4, 5};
```

#### 7.3 배열 접근

```c
int arr[5] = {10, 20, 30, 40, 50};

arr[0] = 10;  // 첫 번째 요소
arr[1] = 20;  // 두 번째 요소
arr[4] = 50;  // 마지막 요소
```

**주의:**
- 인덱스는 0부터 시작
- 배열 크기를 벗어나면 안 됨 (오류 발생 가능)

#### 7.4 배열과 반복문

```c
int arr[5] = {1, 2, 3, 4, 5};
int i;

for (i = 0; i < 5; i++) {
    printf("%d ", arr[i]);
}
```

### 🧪 실습

#### 실습 1: 배열 기본 사용

```c
#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};
    int i;
    
    for (i = 0; i < 5; i++) {
        printf("arr[%d] = %d\n", i, arr[i]);
    }
    
    return 0;
}
```

#### 실습 2: 배열 입력받기

```c
#include <stdio.h>

int main() {
    int arr[5];
    int i;
    
    printf("5개의 정수를 입력하세요: ");
    for (i = 0; i < 5; i++) {
        scanf("%d", &arr[i]);
    }
    
    printf("입력받은 값: ");
    for (i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    return 0;
}
```

#### 실습 3: 배열 합계 계산

```c
#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};
    int i, sum = 0;
    
    for (i = 0; i < 5; i++) {
        sum += arr[i];
    }
    
    printf("합계: %d\n", sum);
    printf("평균: %.2f\n", (float)sum / 5);
    
    return 0;
}
```

#### 실습 4: 최댓값 찾기 (작업형 핵심)

```c
#include <stdio.h>

int main() {
    int arr[5] = {10, 5, 20, 8, 15};
    int i, max = arr[0];
    
    for (i = 1; i < 5; i++) {
        if (arr[i] > max) {
            max = arr[i];
        }
    }
    
    printf("최댓값: %d\n", max);
    
    return 0;
}
```

#### 실습 5: 최솟값 찾기

```c
#include <stdio.h>

int main() {
    int arr[5] = {10, 5, 20, 8, 15};
    int i, min = arr[0];
    
    for (i = 1; i < 5; i++) {
        if (arr[i] < min) {
            min = arr[i];
        }
    }
    
    printf("최솟값: %d\n", min);
    
    return 0;
}
```

#### 실습 6: 배열에서 값 검색

```c
#include <stdio.h>

int main() {
    int arr[5] = {10, 5, 20, 8, 15};
    int i, search, found = 0;
    
    printf("검색할 값: ");
    scanf("%d", &search);
    
    for (i = 0; i < 5; i++) {
        if (arr[i] == search) {
            printf("%d는 %d번째 위치에 있습니다.\n", search, i);
            found = 1;
            break;
        }
    }
    
    if (!found) {
        printf("%d를 찾을 수 없습니다.\n", search);
    }
    
    return 0;
}
```

#### 실습 7: 배열 역순 출력

```c
#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    int i;
    
    printf("원본: ");
    for (i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    printf("역순: ");
    for (i = 4; i >= 0; i--) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    return 0;
}
```

### 🎯 작업형 핵심 문제

#### 문제 1: 배열 평균 계산
```c
// 배열의 평균을 계산하는 함수 작성
```

#### 문제 2: 배열 정렬 (선택 정렬)
```c
// 배열을 오름차순으로 정렬
```

#### 문제 3: 배열에서 중복 제거
```c
// 배열에서 중복된 값을 제거
```

### 🎯 핵심 정리
- 배열 인덱스는 0부터 시작
- 배열 크기를 벗어나면 안 됨
- 반복문과 함께 사용하면 강력함
- 최댓값, 최솟값, 검색 등이 자주 출제됨

### 📝 연습 문제
1. 10개의 정수를 입력받아 평균 계산
2. 배열에서 최댓값과 최솟값 찾기
3. 배열에서 특정 값의 개수 세기

---

## 📅 8주차 – 문자열

### 📌 학습 목표
- 문자 배열로 문자열 표현
- 문자열 함수 사용법
- 문자열 입력/출력 방법

### 📘 이론

#### 8.1 문자열이란?
- C 언어에는 문자열 타입이 없음
- **문자 배열**로 문자열 표현
- 마지막에 `\0` (널 문자)로 끝남

#### 8.2 문자열 선언

```c
// 방법 1: 문자 배열
char str[20];

// 방법 2: 초기화
char str[] = "Hello";

// 방법 3: 크기 지정
char str[20] = "Hello";
```

#### 8.3 문자열 입력

**scanf 사용:**
```c
char name[20];
scanf("%s", name);  // & 없음!
```

**fgets 사용 (권장):**
```c
char name[20];
fgets(name, 20, stdin);  // 공백 포함 입력 가능
```

**주의:**
- `gets()`는 사용하지 않음 (보안 문제)
- `fgets()`는 `\n`도 포함하므로 주의

#### 8.4 문자열 함수

| 함수 | 기능 | 예시 |
|------|------|------|
| `strlen()` | 문자열 길이 | `strlen("Hello")` → 5 |
| `strcpy()` | 문자열 복사 | `strcpy(dest, src)` |
| `strcat()` | 문자열 연결 | `strcat(str1, str2)` |
| `strcmp()` | 문자열 비교 | `strcmp(str1, str2)` |

**헤더 파일:**
```c
#include <string.h>  // 문자열 함수 사용 시
```

### 🧪 실습

#### 실습 1: 문자열 기본

```c
#include <stdio.h>

int main() {
    char name[20] = "홍길동";
    
    printf("이름: %s\n", name);
    
    return 0;
}
```

#### 실습 2: 문자열 입력 (scanf)

```c
#include <stdio.h>

int main() {
    char name[20];
    
    printf("이름을 입력하세요: ");
    scanf("%s", name);  // 공백 전까지만 입력됨
    printf("입력한 이름: %s\n", name);
    
    return 0;
}
```

#### 실습 3: 문자열 입력 (fgets)

```c
#include <stdio.h>
#include <string.h>

int main() {
    char name[20];
    
    printf("이름을 입력하세요: ");
    fgets(name, 20, stdin);
    
    // fgets는 \n도 포함하므로 제거
    name[strlen(name) - 1] = '\0';
    
    printf("입력한 이름: %s\n", name);
    
    return 0;
}
```

#### 실습 4: 문자열 길이

```c
#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello World";
    
    printf("문자열: %s\n", str);
    printf("길이: %d\n", strlen(str));
    
    return 0;
}
```

#### 실습 5: 문자열 복사

```c
#include <stdio.h>
#include <string.h>

int main() {
    char src[] = "Hello";
    char dest[20];
    
    strcpy(dest, src);
    printf("복사된 문자열: %s\n", dest);
    
    return 0;
}
```

#### 실습 6: 문자열 연결

```c
#include <stdio.h>
#include <string.h>

int main() {
    char str1[20] = "Hello";
    char str2[] = " World";
    
    strcat(str1, str2);
    printf("연결된 문자열: %s\n", str1);
    
    return 0;
}
```

#### 실습 7: 문자열 비교

```c
#include <stdio.h>
#include <string.h>

int main() {
    char str1[] = "Hello";
    char str2[] = "Hello";
    char str3[] = "World";
    
    if (strcmp(str1, str2) == 0) {
        printf("str1과 str2는 같습니다.\n");
    }
    
    if (strcmp(str1, str3) != 0) {
        printf("str1과 str3는 다릅니다.\n");
    }
    
    return 0;
}
```

**strcmp 반환값:**
- 같으면: 0
- str1이 크면: 양수
- str1이 작으면: 음수

#### 실습 8: 문자열 검색

```c
#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello World";
    char search = 'o';
    int i, found = 0;
    
    for (i = 0; i < strlen(str); i++) {
        if (str[i] == search) {
            printf("'%c'를 %d번째 위치에서 찾았습니다.\n", search, i);
            found = 1;
        }
    }
    
    if (!found) {
        printf("'%c'를 찾을 수 없습니다.\n", search);
    }
    
    return 0;
}
```

### 🎯 핵심 정리
- 문자열은 문자 배열로 표현
- 마지막에 `\0` (널 문자) 필수
- `fgets()` 사용 권장 (공백 포함 입력 가능)
- `strlen()`, `strcpy()`, `strcat()`, `strcmp()` 자주 사용

### 📝 연습 문제
1. 이름을 입력받아 길이 출력
2. 두 문자열을 입력받아 연결하여 출력
3. 문자열에서 특정 문자 개수 세기

---
