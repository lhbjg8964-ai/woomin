# Java 연습 문제 (2026-07-21)

> 오늘 배운 **변수, 자료형, 산술 연산자, 증감 연산자** 내용을 복습하는 문제입니다.

---

## 1. 변수

### 문제 1-1 (OX)
다음 중 올바른 변수 선언은 O, 잘못된 선언은 X로 표시하세요.

1. `int age = 25;`
2. `int 1age = 25;`
3. `int myAge = 25;`
4. `int class = 25;`
5. `int _score = 80;`
6. `int $price = 1000;`
7. `int my age = 25;`

### 문제 1-2 (서술형)
변수를 선언하고 값을 변경하는 코드를 작성하세요.

- `score`라는 정수 변수를 `80`으로 선언
- `score` 값을 `100`으로 변경
- `"점수 : "` 와 함께 출력

---

## 2. 자료형

### 문제 2-1 (객관식)
`10000000000`을 저장하기에 적합한 자료형은?

1. `int`
2. `long`
3. `byte`
4. `short`

### 문제 2-2 (객관식)
다음 코드 중 컴파일 에러가 발생하는 것은?

1. `float f = 3.14F;`
2. `long l = 10000000000L;`
3. `double d = 3.14;`
4. `float f = 3.14;`

### 문제 2-3 (빈칸 채우기)
아래 코드의 빈칸을 채우세요.

```java
// 1byte, -128 ~ 127
_____ byteVar = 127;

// 8byte, 아주 큰 정수
long population = 7800000000_____;

// float 접미사
float weight = 70.5_____;

// 상수 선언
_____ double PI = 3.14159;
```

### 문제 2-4 (코드 작성)
자기소개 프로그램을 작성하세요.

- `String name`, `int age`, `double height`, `boolean isStudent`, `char grade` 변수 사용
- `final`로 `MAX_SCORE = 100` 상수 선언
- 아래 형식으로 출력

```
이름: 홍길동
나이: 25
키: 175.5cm
학생 여부: true
등급: A
최고 점수: 100
```

---

## 3. 산술 연산자

### 문제 3-1 (출력 결과 예측)
`a = 10`, `b = 3`일 때 각 식의 결과를 적으세요.

| 식 | 결과 |
|----|------|
| `a + b` | |
| `a - b` | |
| `a * b` | |
| `a / b` | |
| `a % b` | |

### 문제 3-2 (출력 결과 예측)
다음 코드의 출력 결과를 적으세요.

```java
int x = 10;
int y = 3;

System.out.println(x / y);
System.out.println((double) x / y);
System.out.println(x / (double) y);
```

### 문제 3-3 (코드 작성)
`num = 17`일 때, 아래 조건을 만족하는 코드를 작성하세요.

1. 2로 나눈 나머지 출력
2. 3으로 나눈 나머지 출력
3. `if`문으로 짝수/홀수 판별 후 출력

---

## 4. 증감 연산자

### 문제 4-1 (출력 결과 예측)
다음 코드 실행 후 `a`, `b`의 값을 적으세요.

```java
int a = 5;
int b = ++a;
```

| 변수 | 값 |
|------|----|
| a | |
| b | |

### 문제 4-2 (출력 결과 예측)
다음 코드 실행 후 `c`, `d`의 값을 적으세요.

```java
int c = 5;
int d = c++;
```

| 변수 | 값 |
|------|----|
| c | |
| d | |

### 문제 4-3 (출력 결과 예측)
다음 코드 실행 후 `e`, `f`의 값을 적으세요.

```java
int e = 10;
int f = --e;
```

| 변수 | 값 |
|------|----|
| e | |
| f | |

### 문제 4-4 (출력 결과 예측)
다음 코드 실행 후 `g`, `h`의 값을 적으세요.

```java
int g = 10;
int h = g--;
```

| 변수 | 값 |
|------|----|
| g | |
| h | |

### 문제 4-5 (객관식)
`int x = 8;` 다음에 `int y = x--;`를 실행하면?

1. x = 7, y = 7
2. x = 8, y = 7
3. x = 7, y = 8
4. x = 8, y = 8

---

## 5. 종합 문제

### 문제 5-1 (코드 작성)
`Rectangle.java` 파일을 만들고, 직사각형의 넓이와 둘레를 계산하세요.

- `final double WIDTH = 10.0;`
- `final double HEIGHT = 5.0;`
- 넓이 = 가로 × 세로
- 둘레 = (가로 + 세로) × 2

**출력 예시:**
```
가로: 10.0
세로: 5.0
넓이: 50.0
둘레: 30.0
```

### 문제 5-2 (코드 작성)
`ScoreCalc.java` 파일을 만들고, 아래 요구사항을 구현하세요.

- 국어 `90`, 영어 `85`, 수학 `78` 점수를 변수로 선언
- 총점과 평균을 계산하여 출력 (평균은 실수로 출력)
- 평균이 80점 이상이면 `"합격"`, 미만이면 `"불합격"` 출력

**출력 예시:**
```
국어: 90
영어: 85
수학: 78
총점: 253
평균: 84.33333333333333
합격
```

### 문제 5-3 (출력 결과 예측)
다음 코드의 출력 결과를 순서대로 적으세요.

```java
int a = 3;
int b = 2;
int c = a++ + ++b;
int d = --a + b--;
System.out.println("c = " + c);
System.out.println("d = " + d);
System.out.println("a = " + a);
System.out.println("b = " + b);
```

---

## 정답

<details>
<summary>1. 변수 정답</summary>

**문제 1-1**
1. O
2. X (숫자로 시작 불가)
3. O
4. X (예약어 사용 불가)
5. O
6. O
7. X (공백 사용 불가)

**문제 1-2**
```java
int score = 80;
score = 100;
System.out.println("점수 : " + score);
```

</details>

<details>
<summary>2. 자료형 정답</summary>

**문제 2-1:** 2 (`long`)

**문제 2-2:** 4 (`float f = 3.14;` — 접미사 F/f 필요)

**문제 2-3:**
```java
byte byteVar = 127;
long population = 7800000000L;
float weight = 70.5f;
final double PI = 3.14159;
```

**문제 2-4 (예시)**
```java
String name = "홍길동";
int age = 25;
double height = 175.5;
boolean isStudent = true;
char grade = 'A';
final int MAX_SCORE = 100;

System.out.println("이름: " + name);
System.out.println("나이: " + age);
System.out.println("키: " + height + "cm");
System.out.println("학생 여부: " + isStudent);
System.out.println("등급: " + grade);
System.out.println("최고 점수: " + MAX_SCORE);
```

</details>

<details>
<summary>3. 산술 연산자 정답</summary>

**문제 3-1**

| 식 | 결과 |
|----|------|
| `a + b` | 13 |
| `a - b` | 7 |
| `a * b` | 30 |
| `a / b` | 3 |
| `a % b` | 1 |

**문제 3-2**
```
3
3.3333333333333335
3.3333333333333335
```

**문제 3-3 (예시)**
```java
int num = 17;
System.out.println(num + " % 2 = " + (num % 2));
System.out.println(num + " % 3 = " + (num % 3));

if (num % 2 == 0) {
    System.out.println(num + " 짝수입니다.");
} else {
    System.out.println(num + " 홀수입니다.");
}
```

</details>

<details>
<summary>4. 증감 연산자 정답</summary>

**문제 4-1:** a = 6, b = 6

**문제 4-2:** c = 6, d = 5

**문제 4-3:** e = 9, f = 9

**문제 4-4:** g = 9, h = 10

**문제 4-5:** 3 (x = 7, y = 8)

</details>

<details>
<summary>5. 종합 문제 정답</summary>

**문제 5-1 (예시)**
```java
public class Rectangle {
    public static void main(String[] args) {
        final double WIDTH = 10.0;
        final double HEIGHT = 5.0;

        double area = WIDTH * HEIGHT;
        double perimeter = (WIDTH + HEIGHT) * 2;

        System.out.println("가로: " + WIDTH);
        System.out.println("세로: " + HEIGHT);
        System.out.println("넓이: " + area);
        System.out.println("둘레: " + perimeter);
    }
}
```

**문제 5-2 (예시)**
```java
public class ScoreCalc {
    public static void main(String[] args) {
        int korean = 90;
        int english = 85;
        int math = 78;

        int total = korean + english + math;
        double average = total / 3.0;

        System.out.println("국어: " + korean);
        System.out.println("영어: " + english);
        System.out.println("수학: " + math);
        System.out.println("총점: " + total);
        System.out.println("평균: " + average);

        if (average >= 80) {
            System.out.println("합격");
        } else {
            System.out.println("불합격");
        }
    }
}
```

**문제 5-3**
```
c = 6
d = 6
a = 3
b = 2
```

**풀이:**
- `c = a++ + ++b` → a=3 사용 후 a=4, b=2→3 증가 → c = 3 + 3 = 6
- `d = --a + b--` → a=4→3 감소, b=3 사용 후 b=2 → d = 3 + 3 = 6

</details>
