# 문제 1 답안 — 영화 대여 관리 시스템

## 실행 방법

```bash
cd d:\junsuk3\java\Hello\src
javac a0811/movie/*.java
java a0811.movie.MovieApp
```

## 도서 관리 시스템과의 대응

| 도서 관리 | 영화 대여 |
|-----------|-----------|
| `Library` | `Movie` |
| `LibraryManger` | `MovieManager` |
| `Search` | `MovieApp` |
| `librarys` | `movies` |
| `bookLocation` | `rentedList` |
| `book()` | `rent()` |
| `available` | `available` |

## 핵심 포인트

- `rent(String title)` / `rent()` 메서드 오버로딩
- 대여 중인 영화는 `deleteMovie()`에서 삭제 불가
- 생성자에서 Inception, Parasite, Toy Story 3편 등록
